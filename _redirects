# Netlify redirects / rewrites
#
# Send any unknown path to /index.html (which will then show the login screen
# if the user isn't signed in, or the hub if they are).
#
# Note the 200 (rewrite, not redirect) — we want the URL to stay what the
# user typed but serve index.html content. This handles typos and stale
# bookmarks gracefully.

/*   /index.html   200
