{
  "name": "performance-property-tools",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Static site for Performance Property internal tools. The Node deps here are only for scripts/render-reports.mjs (the monthly GitHub Actions renderer).",
  "scripts": {
    "render-reports": "node scripts/render-reports.mjs",
    "seed-scrabble-words": "node scripts/seed-scrabble-words.mjs",
    "check": "node scripts/check-static.mjs"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.39.7",
    "puppeteer": "^22.15.0"
  }
}
